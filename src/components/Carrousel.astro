---
import BtnShadow from "./BtnShadow.astro"
---

<div id="imgContainer" class="grid rounded-lg">
    <div class="imgSlide">
        <img src="/branding.png" alt="Servicio profesional de Branding de Marcas.">
        <BtnShadow 
        href="#" >
            Branding
        </BtnShadow>
    </div>
    <div class="imgSlide">
        <img src="/events.png" alt="Servicio profesional de Branding de Marcas.">
        <BtnShadow 
        href="#" >
            Eventos
        </BtnShadow>
    </div>
    <div class="imgSlide">
        <img src="/graphic-design.png" alt="Servicio profesional de Branding de Marcas.">        
        <BtnShadow 
        href="#" 
        >
            Diseño Gráfico
        </BtnShadow>
    </div>
    <div class="imgSlide">
        <img src="/marketing_digital.png" alt="Servicio profesional de Branding de Marcas.">
        <BtnShadow 
        href="#">
            Marketing Digital
        </BtnShadow>
    </div>
    <div class="imgSlide">
        <img src="/photography.png" alt="Servicio profesional de Branding de Marcas.">
        <BtnShadow 
        href="#">
            Fotografía
        </BtnShadow>
    </div>
    <div class="imgSlide">
        <img src="/web-app.png" alt="Servicio profesional de Branding de Marcas.">
        <BtnShadow 
        href="#">
            Aplicaciones Web
        </BtnShadow>
    </div>
    <div class="imgSlide">
        <img src="/3d.png" alt="Servicio profesional de Branding de Marcas.">
        <BtnShadow 
        href="#">
            3D
        </BtnShadow>
    </div>
    
</div>

<style>
    #imgContainer{
        position: relative;
        overflow-y: scroll;
        scroll-snap-type: y mandatory;
        height: 15rem;
        max-height: 20rem;
        max-width: 90%;
        margin: auto;
    }
    #imgContainer::-webkit-scrollbar{
        display: none;
    }
    .imgSlide{
        min-width: 100%;
        scroll-snap-align: center;
        position: sticky;
        height: 15rem;
    }
    img{
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 0.5rem;
    }
    a{
        position: absolute;
        padding: 0.5rem 1rem;
        background: var(--dark-blur-background);
        backdrop-filter: blur(4px);
        inset: 0;
        top: 10rem;
        margin: auto;
        border-radius: 0.125rem;
        color: var(--dark-accent);
        font-size: 1.125rem;
        line-height: 1.75rem;
        transform: translate(0,0) scale(1);
        border: 1px solid var(--dark-accent);
        box-shadow: 0px 0px 0px var(--dark-background);
        transition-duration: 300ms;
        height: max-content;
        width: max-content;
    }
    a:hover{
        background: var(--dark-accent);
        transform: translate(0.5rem, -0.5rem);
        color: var(--dark-background);
        box-shadow: -3px 3px 0px var(--dark-background);
    }
    a:active{
        transform: scale(0.9);
        box-shadow: -1px 1px 0px var(--dark-background);
    }
    @media (width > 767px) {

        #imgContainer, .imgSlide{
            max-height: 20rem;
            max-width: 100%;
        }
        .imgSlide{
            margin: 0;
        }
    }
</style>